  <h1>Jot</h1>
  <%= link_to "New", new_document_path, class: "button button-primary", id: "new_doc_link", remote: true %>
  <hr>
  <ul>
    <% @documents.each do |doc| %>
      <li><a href="/documents/<%= doc.key %>"><%= doc.value['title'] %></a></li>
    <% end %>
  </ul>

